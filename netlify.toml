[build]
  command = "pnpm --filter @obraflux/shared build && pnpm --filter web build"
  publish = "apps/web/.next"

[build.environment]
  NEXT_PUBLIC_API_URL = "https://obrafluxapi-production.up.railway.app/api"
  NODE_VERSION = "20"
